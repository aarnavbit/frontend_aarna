Aim:This Documnet is the uniform naminig convention of the project
Constraints:
Variables: lowercase (e.g., username, count, filepath)
Classes: UPPERCASE (e.g., USERMANAGER, DATABASE, CONFIG)
Functions: PascalCase (e.g., CreateUser(), LoadConfig(), SaveFile())
Rules:
No Shortcuts for the names, if userpassword then it shoudl be only userpassword and not userpass
Highfens to be only used when the function  or the class does multiple actions 
